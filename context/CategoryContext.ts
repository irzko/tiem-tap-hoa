import { createContext } from "react";

const CategoryContext = createContext({
  // data: [] as any,
  // mutate: (key: string) => {},
  // parentId: null,
} as any);

export default CategoryContext;